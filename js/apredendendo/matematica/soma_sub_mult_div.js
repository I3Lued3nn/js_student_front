let alunos = 20;

//alunos = alunos + 1 ; // SOMA | alunos += 1;
// alunos = alunos - 1 ; DIVISÃO | alunos -= 1;
// alunos = alunos * 2 ; MULTIPLICAÇÃO | alunos *= 2;
// alunos = alunos / 2 ; DIVISÃO | alunos /= 2;
// alunos = alunos ** 2; exponencial | alunos **= 1;
//let extra_alunos = alunos % 3; // resto de divião

// outro método
alunos += 1;

console.log(alunos);
//console.log(extra_alunos);

/*
    opradores precedentes
    1 - ()
    2 - exponenciais
    3 - multiplicação e divisão
    4 - soma e subtração
 */

let resultado = 1 + 2 * (3 + 4)

console.log(resultado);