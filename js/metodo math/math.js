let x = 3 ;
let y = 2 ;
let a = 81;
let b = 10;
let c = 45;
let z;

/*
z = Math.round(x);        // Retorna x arredondado para o número inteiro mais próximo
z = Math.ceil   	//Retorna x arredondado para o número inteiro mais próximo
z = Math.floor   	//Retorna x arredondado para o número inteiro mais próximo
z = Math.trunc(x)    	//Retorna a parte inteira de x ( novo no ES6 )
*/
//z = Math.pow(x, y); // numerado expoente 
//z = Math.sqrt(a) // descobrir a raiz 
//z = Math.log(b); // logaritmo 
//z = Math.sin(c);//seno
//z = Math.cos(c);//cosceno
//z = Math.tan(c);//tangente


let max = Math.max(x,y,a,b,c);

let min = Math.min(x,y,a,b,c);
console.log(min)